box: martinsthiago/nvm-browsers

build:
  steps:
    - script:
        name: dependencies installation
        code: |
          npm install -g bower web-component-tester -s
          bower install --allow-root
    - script:
        name: tests execution
        code: |
          wct