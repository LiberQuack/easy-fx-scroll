box: martinsthiago/nvm-browsers

build:
  steps:
    - script:
        name: dependencies installation
        code: |
          sudo npm install -g bower web-component-tester -s
          sudo bower install --allow-root
    - script:
        name: tests execution
        code: |
          wct